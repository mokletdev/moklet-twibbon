/**
 * Object names used to identify elements on the canvas
 */
export const OBJECT_NAMES = {
  /** Identifier for the frame layer */
  FRAME: "twibbon_frame",
  /** Identifier for the background layer */
  BACKGROUND: "twibbon_background",
};

/**
 * Default canvas size for desktop devices (pixels)
 */
export const DEFAULT_CANVAS_SIZE = 500;

/**
 * Default canvas size for mobile devices (pixels)
 */
export const MOBILE_CANVAS_SIZE = 300;

/**
 * Default scale factor for frame images (0-1)
 */
export const DEFAULT_SCALE = 0.5;

/**
 * Default value for brightness and contrast filters
 */
export const DEFAULT_FILTER_VALUE = 0;

/**
 * Media query breakpoint for desktop devices (pixels)
 */
export const DESKTOP_BREAKPOINT = 768;

/**
 * Image quality setting for exported images (1-4)
 */
export const EXPORT_IMAGE_QUALITY = 2;

/**
 * Export image multiplier for higher resolution output
 */
export const EXPORT_MULTIPLIER = 4;

/**
 * Delay in milliseconds to wait for canvas initialization
 */
export const CANVAS_INIT_DELAY = 50;

/**
 * Background color for the canvas
 */
export const CANVAS_BACKGROUND_COLOR = "#EEEEF3";

/**
 * Blur amount for background when blur effect is applied
 */
export const BACKGROUND_BLUR_AMOUNT = 0.5;
